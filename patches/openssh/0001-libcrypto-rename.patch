diff --git a/configure.ac b/configure.ac
index a8e9df66b..ed2528dcc 100644
--- a/configure.ac
+++ b/configure.ac
@@ -3015,8 +3015,8 @@ AC_ARG_WITH([ssl-engine],
 
 nocrypto_saved_LIBS="$LIBS"
 if test "x$openssl" = "xyes" ; then
-	LIBS="-lcrypto $LIBS"
-	CHANNELLIBS="-lcrypto $CHANNELLIBS"
+	LIBS="-lcrypto_internal $LIBS"
+	CHANNELLIBS="-lcrypto_internal $CHANNELLIBS"
 	AC_TRY_LINK_FUNC([RAND_add], , [
 	    # As of early 2026, BoringSSL libcrypto needs -lstdc++ for
 	    # destructors so try that before giving up.
